# راهنمای مشارکت در پروژه LPIC-1

خوش آمدید! از علاقه شما به مشارکت در این پروژه سپاسگزاریم. این راهنما به شما کمک می‌کند تا به درستی در پروژه مشارکت کنید.

## 📋 فهرست مطالب

- [ساختار پروژه](#ساختار-پروژه)
- [اضافه کردن درس جدید](#اضافه-کردن-درس-جدید)
- [استفاده از قالب درس](#استفاده-از-قالب-درس)
- [به‌روزرسانی ناوبری](#به‌روزرسانی-ناوبری)
- [به‌روزرسانی جستجو](#به‌روزرسانی-جستجو)
- [استانداردهای نگارش](#استانداردهای-نگارش)
- [فرآیند بررسی](#فرآیند-بررسی)

## 📁 ساختار پروژه

```
LPIC1/
├── content/              # محتوای دروس
│   ├── 101/             # دروس موضوع 101
│   ├── 102/             # دروس موضوع 102
│   ├── ...
│   ├── 110/             # دروس موضوع 110
│   ├── _template.md     # قالب استاندارد درس
│   ├── index.md         # صفحه اصلی
│   ├── lessons.md       # فهرست دروس
│   └── assets/          # CSS/JS سفارشی
├── theme/               # قالب سفارشی MkDocs
├── site/                # فایل‌های ساخته شده (generated)
├── scripts/             # اسکریپت‌های کمکی
├── mkdocs.yml           # پیکربندی MkDocs
└── requirements.txt     # وابستگی‌ها
```

## ➕ اضافه کردن درس جدید

### مرحله 1: انتخاب موضوع و شماره

دروس LPIC-1 بر اساس شماره موضوعات سازماندهی شده‌اند:

- **101:** معماری سیستم
- **102:** نصب Linux و مدیریت بسته‌ها
- **103:** دستورات GNU و Unix
- **104:** دستگاه‌ها و فایل‌سیستم‌ها
- **105:** Shell و Scripting
- **106:** رابط‌های کاربری
- **107:** وظایف مدیریتی
- **108:** سرویس‌های سیستم
- **109:** مبانی شبکه
- **110:** امنیت

مثال: برای درس "103.7 Search text files using regular expressions"، فایل باید در پوشه `content/103/` با نام `103-7-search-text-files-using-regular-expressions.md` ایجاد شود.

### مرحله 2: کپی کردن قالب

```bash
# کپی قالب به مکان مناسب با نام صحیح
cp content/_template.md content/103/103-7-search-text-files-using-regular-expressions.md
```

### مرحله 3: پر کردن محتوا

فایل قالب (`content/_template.md`) شامل تمام بخش‌های مورد نیاز است:

1. **عنوان و اطلاعات پایه:** وزن، توضیح، اهداف
2. **حوزه‌های کلیدی دانش:** نکات کلیدی که باید یاد گرفته شود
3. **اصطلاحات و ابزارها:** لیست دستورات و فایل‌های مهم
4. **توضیحات:** محتوای اصلی درس
5. **دستورات مهم:** شرح دستورات با مثال
6. **مثال‌ها:** مثال‌های عملی
7. **نکات:** نکات مهم، هشدارها، اطلاعات بیشتر
8. **خلاصه:** خلاصه کلی درس
9. **تمرین عملی:** تمرین‌های پیشنهادی

## 📝 استفاده از قالب درس

قالب استاندارد در `content/_template.md` موجود است. این قالب شامل:

### ساختار عنوان

```markdown
# عنوان درس

**وزن:** [وزن درس از 1 تا 5]
**توضیح:** [توضیح کوتاه درس]
```

### بخش هدف

```markdown
## هدف

[اهداف یادگیری این درس را به فارسی شرح دهید]
```

### دستورات و کدها

```markdown
## دستورات مهم

### نام دستور

\`\`\`bash
command --option argument
\`\`\`

**توضیح:** [توضیح دستور]
```

### نکات و هشدارها

```markdown
!!! tip "نکته مهم"
    [نکته‌های کاربردی]

!!! warning "توجه"
    [هشدارها]

!!! info "اطلاعات بیشتر"
    [اطلاعات تکمیلی]
```

## 🧭 به‌روزرسانی ناوبری

پس از اضافه کردن درس جدید، باید دو فایل را به‌روزرسانی کنید:

### 1. فایل `mkdocs.yml`

این فایل ناوبری سایت را کنترل می‌کند:

```yaml
nav:
  - خانه: index.md
  - فهرست دروس: lessons.md
  - دروس:
    - مبانی لینوکس: basics.md
    - مجوزهای فایل: permissions.md
    - درس جدید شما: 103/103-7-search-text-files-using-regular-expressions.md
```

**نکته:** مسیر فایل نسبت به پوشه `content/` است.

### 2. فایل `content/lessons.md`

این فایل لیست کامل دروس را نمایش می‌دهد. درس جدید را به بخش مربوطه اضافه کنید:

```markdown
### موضوع 103: دستورات GNU و Unix

- [103.1 کار با خط فرمان](103/103-1-work-on-the-command-line.md)
- [103.7 جستجوی متن با عبارات منظم](103/103-7-search-text-files-using-regular-expressions.md) ← جدید
```

## 🔍 به‌روزرسانی جستجو

### جستجوی خودکار

MkDocs Material به طور خودکار ایندکس جستجو را از فایل‌های Markdown ایجاد می‌کند. نیازی به کار دستی نیست!

### بازسازی ایندکس جستجو

هر بار که سایت را build می‌کنید، ایندکس جستجو به‌روزرسانی می‌شود:

```bash
# ساخت سایت (ایندکس جستجو به‌روزرسانی می‌شود)
mkdocs build --clean
```

### تست جستجو در محیط محلی

```bash
# اجرای سرور محلی
mkdocs serve

# سایت را در مرورگر باز کنید: http://127.0.0.1:8000
# از قابلیت جستجو استفاده کنید تا از صحت عملکرد آن مطمئن شوید
```

## 📖 استانداردهای نگارش

### زبان و لحن

1. **فارسی ساده و روان:** از زبان ساده و قابل فهم استفاده کنید
2. **لحن آموزشی:** مانند یک معلم صبور و دوستانه بنویسید
3. **مثال‌محور:** هر مفهوم را با مثال‌های عملی توضیح دهید

### فرمت‌بندی

1. **اصطلاحات فنی:** دستورات، نام فایل‌ها، و مسیرها را به انگلیسی نگه دارید
   ```
   ✅ دستور `ls -la` فایل‌ها را نمایش می‌دهد
   ❌ دستور ال‌اس منهای ال‌ای فایل‌ها را نمایش می‌دهد
   ```

2. **کدها:** همیشه از code blocks استفاده کنید
   ```bash
   ls -la /home
   ```

3. **جداول:** برای مقایسه از جداول استفاده کنید
   ```markdown
   | گزینه | توضیح |
   |--------|--------|
   | -l     | نمایش جزئیات |
   | -a     | نمایش فایل‌های مخفی |
   ```

4. **لیست‌ها:** برای ترتیب مراحل از لیست شماره‌دار استفاده کنید

### محتوا

1. **جامع بودن:** تمام جنبه‌های موضوع را پوشش دهید
2. **دقیق بودن:** اطلاعات تکنیکی باید دقیق و صحیح باشد
3. **عملی بودن:** مثال‌های واقعی و کاربردی ارائه دهید
4. **سازمان‌یافته بودن:** از ساختار منطقی و سلسله‌مراتبی استفاده کنید

## 🔄 فرآیند مشارکت

### 1. Fork و Clone

```bash
# Fork کردن مخزن در GitHub
# سپس clone کردن fork خود
git clone https://github.com/YOUR_USERNAME/LPIC1-.git
cd LPIC1-
```

### 2. ایجاد Branch

```bash
# برای هر درس یا تغییر، branch جدید ایجاد کنید
git checkout -b add-lesson-103-7
```

### 3. ایجاد یا ویرایش محتوا

- از قالب استاندارد استفاده کنید
- استانداردهای نگارش را رعایت کنید
- ناوبری را به‌روزرسانی کنید

### 4. تست محلی

```bash
# نصب وابستگی‌ها
pip install -r requirements.txt

# اجرای سرور محلی
mkdocs serve

# بررسی سایت در مرورگر: http://127.0.0.1:8000
# بررسی موارد زیر:
# - درس جدید در ناوبری نمایش داده می‌شود؟
# - فرمت‌بندی صحیح است؟
# - لینک‌ها کار می‌کنند؟
# - جستجو درس جدید را پیدا می‌کند؟
```

### 5. Commit و Push

```bash
# اضافه کردن تغییرات
git add .

# commit با پیام معنادار
git commit -m "Add lesson 103.7: Search text files using regular expressions"

# push به fork خود
git push origin add-lesson-103-7
```

### 6. ایجاد Pull Request

1. به مخزن اصلی در GitHub بروید
2. Pull Request جدید ایجاد کنید
3. توضیح کاملی از تغییرات ارائه دهید
4. منتظر بررسی و بازخورد باشید

## ✅ چک‌لیست قبل از Pull Request

قبل از ارسال Pull Request، موارد زیر را بررسی کنید:

- [ ] درس جدید از قالب استاندارد پیروی می‌کند
- [ ] فایل `mkdocs.yml` به‌روزرسانی شده است
- [ ] فایل `content/lessons.md` به‌روزرسانی شده است
- [ ] تمام لینک‌ها کار می‌کنند
- [ ] کدها به درستی فرمت شده‌اند
- [ ] املا و دستور زبان فارسی صحیح است
- [ ] سایت به صورت محلی تست شده است
- [ ] جستجو درس جدید را پیدا می‌کند

## 🐛 گزارش مشکلات

اگر مشکلی پیدا کردید:

1. ابتدا [Issues](https://github.com/alireza-haeri/LPIC1-/issues) موجود را بررسی کنید
2. اگر مشکل گزارش نشده، Issue جدید ایجاد کنید
3. توضیح واضحی از مشکل ارائه دهید
4. در صورت امکان، اسکرین‌شات یا مثال اضافه کنید

## 💡 پیشنهادات

برای پیشنهاد ویژگی‌های جدید یا بهبودها:

1. Issue جدید با برچسب "enhancement" ایجاد کنید
2. توضیح دهید چرا این تغییر مفید است
3. در صورت امکان، راه‌حل پیشنهادی ارائه دهید

## 📞 سوالات

اگر سوالی دارید:

- از بخش [Discussions](https://github.com/alireza-haeri/LPIC1-/discussions) استفاده کنید
- یا در Issues سوال خود را مطرح کنید

## 🙏 تشکر

از مشارکت شما در بهبود این پروژه سپاسگزاریم! هر کمکی، کوچک یا بزرگ، ارزشمند است.

---

**با هم یک منبع آموزشی عالی برای جامعه لینوکس ایران می‌سازیم! 🚀**
