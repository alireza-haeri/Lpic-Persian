# راهنمای ترجمه مستندات LPIC-1

این راهنما دستورالعمل‌های کامل برای ترجمه فایل‌های مارک‌داون LPIC-1 از انگلیسی به فارسی را ارائه می‌دهد.

## اصول کلی ترجمه

### 1. زبان و لحن

**باید:**
- از فارسی ساده و روان استفاده کنید
- مانند یک معلم با دانش‌آموز صحبت کنید
- توضیحات واضح و قابل فهم بدهید
- از اصطلاحات رایج فارسی استفاده کنید

**نباید:**
- از زبان بسیار رسمی و پیچیده استفاده کنید
- ترجمه کلمه به کلمه انجام دهید
- جملات را طولانی و گنگ کنید

**مثال خوب:**
```
✓ "وقتی دستور ls را اجرا می‌کنید، لیست فایل‌ها نمایش داده می‌شود"
✗ "هنگامی که فرمان ls را به اجرا در می‌آورید، فهرست پرونده‌ها ارائه می‌گردد"
```

### 2. اصطلاحات فنی

**هرگز ترجمه نکنید:**
- نام دستورات: `ls`, `cd`, `grep`, `awk`, etc.
- نام فایل‌ها: `/etc/passwd`, `~/.bashrc`
- مسیرها (paths): `/home/user/documents`
- متغیرهای محیطی: `$PATH`, `$HOME`
- نام برنامه‌ها: `systemd`, `GRUB`, `Apache`
- پارامترها و سوییچ‌ها: `-l`, `--help`, `-rf`

**ترجمه با توضیح:**
```markdown
استفاده از دستور `ls` برای لیست کردن فایل‌ها
```

### 3. ساختار Markdown

**حفظ کنید:**
- سرفصل‌ها: `# عنوان اصلی`, `## عنوان فرعی`
- لیست‌ها: `- آیتم` یا `1. آیتم`
- بلوک‌های کد: <code>```bash</code>
- جداول: ساختار `|` و `-`
- لینک‌ها: `[متن](url)`
- تاکید: `**bold**`, `*italic*`

**مثال بلوک کد:**
````markdown
```bash
$ ls -la
drwxr-xr-x 2 user user 4096 Jan 1 10:00 Documents
```
````

**نکته مهم:** محتوای داخل بلوک‌های کد را هرگز ترجمه نکنید!

### 4. ساختار استاندارد فایل

هر فایل ترجمه شده باید این ساختار را داشته باشد:

```markdown
# [عنوان به فارسی]

**وزن:** [عدد]
**توضیح:** [توضیح به فارسی]

## حوزه‌های کلیدی دانش
- [موضوع 1]
- [موضوع 2]

## اصطلاحات و ابزارها
- command1
- command2
- /path/to/file

## [محتوای اصلی]

[متن ترجمه شده با حفظ ساختار]

---

**مثال عملی:** [یک مثال کاربردی اضافه کنید]

**خلاصه:** [خلاصه‌ای از کل موضوع در 2-3 جمله]

---

[لینک‌های ناوبری به فارسی]

**دسته‌بندی:** LPIC1
**برچسب‌ها:** [برچسب‌ها]
**تماس**
```

## راهنمای گام به گام

### مرحله 1: آماده‌سازی

1. فایل انگلیسی را باز کنید
2. ساختار کلی را بررسی کنید
3. بلوک‌های کد را شناسایی کنید
4. اصطلاحات فنی را علامت‌گذاری کنید

### مرحله 2: ترجمه عنوان و مقدمه

```markdown
# 103.1 Work on the command line
↓ ترجمه ↓
# 103.1 کار بر روی خط فرمان
```

```markdown
Weight: 4
Description: Candidates should be able to interact with shells...
↓ ترجمه ↓
**وزن:** 4
**توضیح:** داوطلبان باید قادر به تعامل با shell ها...
```

### مرحله 3: ترجمه محتوا

**قوانین:**
- جملات را طبیعی و روان کنید
- از ساختار فارسی استفاده کنید (نه ترجمه مستقیم)
- توضیحات اضافی در صورت نیاز بدهید

**مثال:**

```markdown
انگلیسی:
"You can use the ls command to list files in a directory."

فارسی (ضعیف):
"شما می‌توانید از دستور ls برای لیست کردن فایل‌ها در یک دایرکتوری استفاده کنید."

فارسی (خوب):
"برای دیدن لیست فایل‌های یک پوشه، از دستور `ls` استفاده کنید."
```

### مرحله 4: مدیریت بلوک‌های کد

**قانون طلایی:** هیچ‌چیز در بلوک کد ترجمه نشود!

```markdown
✓ درست:
برای دیدن اطلاعات کامل از `-l` استفاده کنید:
```bash
$ ls -l /home
total 4
drwxr-xr-x 2 user user 4096 Jan 1 10:00 user
```

✗ غلط:
برای دیدن اطلاعات کامل از `-l` استفاده کنید:
```bash
$ ls -l /خانه
مجموع 4
drwxr-xr-x 2 کاربر کاربر 4096 ژانویه 1 10:00 کاربر
```
```

### مرحله 5: اضافه کردن مثال عملی

در پایان هر موضوع، یک مثال کاربردی اضافه کنید:

```markdown
**مثال عملی:** اگر می‌خواهید فایل‌های پنهان را نیز ببینید:
```bash
$ ls -la
```
این دستور تمام فایل‌ها از جمله فایل‌های پنهان (که با `.` شروع می‌شوند) را نمایش می‌دهد.
```

### مرحله 6: نوشتن خلاصه

در پایان فایل، خلاصه‌ای جامع بنویسید:

```markdown
**خلاصه:** دستور `ls` برای لیست کردن فایل‌ها استفاده می‌شود. 
با گزینه `-l` اطلاعات کامل، با `-a` فایل‌های پنهان و با `-h` اندازه 
خوانا نمایش داده می‌شود. ترکیب `ls -lah` پرکاربردترین حالت است.
```

## نکات خاص

### ترجمه جداول

جداول را با همان فرمت حفظ کنید:

```markdown
| سوییچ | توضیحات |
|-------|----------|
| `-l` | نمایش اطلاعات کامل |
| `-a` | نمایش فایل‌های پنهان |
| `-h` | اندازه قابل خواندن |
```

### ترجمه لیست‌ها

```markdown
حالت‌های مختلف boot:
- **حالت عادی:** راه‌اندازی کامل سیستم
- **حالت single-user:** برای تعمیر و نگهداری
- **حالت rescue:** برای بازیابی سیستم
```

### مدیریت نقل‌قول‌ها

```markdown
همانطور که در مستندات آمده: "GRUB is a bootloader"
↓ ترجمه ↓  
همانطور که در مستندات آمده: "GRUB یک bootloader است"
```

## چک‌لیست نهایی

قبل از ارسال فایل، بررسی کنید:

- [ ] تمام سرفصل‌ها ترجمه شده‌اند
- [ ] بلوک‌های کد دست نخورده باقی مانده‌اند
- [ ] اصطلاحات فنی حفظ شده‌اند
- [ ] ساختار markdown صحیح است
- [ ] لینک‌های ناوبری ترجمه شده‌اند
- [ ] مثال عملی اضافه شده
- [ ] خلاصه نوشته شده
- [ ] املا و گرامر فارسی صحیح است
- [ ] فایل با فارسی RTL نمایش داده می‌شود

## ابزارهای کمکی

### بررسی فایل

```bash
# بررسی که فایل UTF-8 است
file -i content/filename.md

# شمارش خطوط
wc -l content/filename.md

# مشاهده پیش‌نمایش
cat content/filename.md
```

### الگوی نام‌گذاری

نام فایل را همانند فایل اصلی حفظ کنید:
```
lpic1-guide/docs/lpic1/103-1-work-on-the-command-line.md
                        ↓
content/103-1-work-on-the-command-line.md
```

## مثال کامل

### فایل اصلی (انگلیسی):

```markdown
# 103.3 Basic File Management

Weight: 4
Candidates should be able to use basic Linux commands to manage files.

Terms
ls
cp
mv

## Listing Files

Use ls to list files:
$ ls -la
```

### فایل ترجمه شده (فارسی):

```markdown
# 103.3 مدیریت پایه فایل‌ها

**وزن:** 4
**توضیح:** داوطلبان باید قادر به استفاده از دستورات پایه Linux برای مدیریت فایل‌ها باشند.

## اصطلاحات
- ls
- cp
- mv

## لیست کردن فایل‌ها

از `ls` برای دیدن لیست فایل‌ها استفاده کنید:

```bash
$ ls -la
```

این دستور تمام فایل‌ها را با جزئیات کامل نمایش می‌دهد.

---

**مثال عملی:** برای دیدن فقط فایل‌های `.txt`:
```bash
$ ls *.txt
```

**خلاصه:** دستورات `ls` برای لیست، `cp` برای کپی و `mv` برای جابجایی فایل‌ها استفاده می‌شوند.

---

**دسته‌بندی:** LPIC1
```

## منابع اضافی

- [مستندات اصلی LPIC](https://www.lpi.org)
- [راهنمای Markdown](https://www.markdownguide.org)
- [فرهنگ اصطلاحات فنی](https://terminology.ir)

## دریافت کمک

اگر در ترجمه مشکل دارید:
1. به فایل‌های ترجمه شده موجود مراجعه کنید
2. الگوها و ساختار را بررسی کنید
3. از این راهنما استفاده کنید

---

**موفق باشید!** 🚀

با رعایت این دستورالعمل‌ها، ترجمه‌های با کیفیت و یکنواخت ایجاد خواهید کرد.
