# راهنمای جامع LPIC-1 📚

وب‌سایت مستندات استاتیک برای یادگیری LPIC-1 به زبان فارسی

## 🌟 ویژگی‌ها

- ✅ پشتیبانی کامل از RTL (راست به چپ) برای محتوای فارسی
- ✅ طراحی مدرن و تمیز با Material for MkDocs
- ✅ ساختار component-based برای سرصفحه، پاورقی، استایل‌ها و اسکریپت‌ها
- ✅ تبدیل خودکار Markdown به HTML
- ✅ نوار جستجوی قدرتمند با پشتیبانی از زبان فارسی
- ✅ منوی ناوبری چسبنده (Sticky Navigation)
- ✅ حالت روشن و تاریک
- ✅ طراحی واکنش‌گرا (Responsive)
- ✅ دکمه کپی کد
- ✅ نوار پیشرفت خواندن

## 📁 ساختار پروژه

```
lpic1-guide/
├── docs/
│   ├── index.md           # صفحه اصلی
│   ├── basics.md          # مبانی لینوکس
│   └── permissions.md     # مجوزهای فایل
├── theme/
│   ├── main.html          # قالب اصلی با بلوک‌های header/footer
│   ├── styles.css         # استایل‌های سفارشی RTL
│   └── script.js          # اسکریپت‌های تعاملی
├── mkdocs.yml             # فایل پیکربندی MkDocs
└── README.md              # این فایل
```

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.8 یا بالاتر
- pip (مدیر بسته‌های Python)

### مراحل نصب

#### 1. نصب MkDocs و Material Theme

```bash
# نصب MkDocs
pip install mkdocs

# نصب Material for MkDocs
pip install mkdocs-material

# نصب پلاگین minify (اختیاری، برای بهینه‌سازی)
pip install mkdocs-minify-plugin
```

یا استفاده از فایل requirements.txt:

```bash
pip install -r requirements.txt
```

#### 2. رفتن به دایرکتوری پروژه

```bash
cd lpic1-guide
```

#### 3. اجرای سرور توسعه محلی

```bash
mkdocs serve
```

سایت شما در آدرس `http://127.0.0.1:8000` در دسترس خواهد بود.

### گزینه‌های اضافی

```bash
# اجرا با پورت سفارشی
mkdocs serve -a 127.0.0.1:8080

# اجرا با حالت verbose
mkdocs serve --verbose

# اجرا با strict mode
mkdocs serve --strict
```

## 🏗️ ساخت سایت استاتیک

برای ساخت نسخه استاتیک سایت:

```bash
mkdocs build
```

فایل‌های HTML تولید شده در دایرکتوری `site/` قرار خواهند گرفت.

### تنظیمات بیلد

```bash
# بیلد با حذف فایل‌های قبلی
mkdocs build --clean

# بیلد با strict mode (توقف در صورت خطا)
mkdocs build --strict

# تعیین دایرکتوری خروجی سفارشی
mkdocs build -d my-site-directory
```

## 📤 استقرار (Deployment)

### روش 1: GitHub Pages (پیشنهادی)

#### استقرار دستی

```bash
# ساخت و استقرار در یک دستور
mkdocs gh-deploy
```

این دستور:
1. سایت را می‌سازد
2. فایل‌ها را به شاخه `gh-pages` push می‌کند
3. GitHub Pages به طور خودکار سایت را منتشر می‌کند

سایت شما در `https://alireza-haeri.github.io/LPIC1-/` در دسترس خواهد بود.

#### استقرار خودکار با GitHub Actions

فایل `.github/workflows/deploy-docs.yml` ایجاد کنید:

```yaml
name: Deploy Documentation

on:
  push:
    branches:
      - main
    paths:
      - 'lpic1-guide/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      
      - name: Install dependencies
        run: |
          pip install mkdocs-material
          pip install mkdocs-minify-plugin
      
      - name: Deploy to GitHub Pages
        run: |
          cd lpic1-guide
          mkdocs gh-deploy --force
```

### روش 2: سرورهای دیگر

فایل‌های ساخته شده در `site/` را به هر سرور وب منتقل کنید:

```bash
# مثال: استقرار با rsync
rsync -avz site/ user@server:/var/www/html/

# مثال: استقرار با scp
scp -r site/* user@server:/var/www/html/
```

### روش 3: Netlify

1. مخزن GitHub را به Netlify متصل کنید
2. تنظیمات Build:
   - **Build command**: `cd lpic1-guide && mkdocs build`
   - **Publish directory**: `lpic1-guide/site`

### روش 4: Vercel

1. مخزن را به Vercel متصل کنید
2. تنظیمات:
   - **Framework Preset**: Other
   - **Build Command**: `cd lpic1-guide && pip install mkdocs-material mkdocs-minify-plugin && mkdocs build`
   - **Output Directory**: `lpic1-guide/site`

## ✏️ افزودن محتوای جدید

### اضافه کردن صفحه جدید

1. فایل Markdown جدید در `docs/` ایجاد کنید:

```bash
touch docs/networking.md
```

2. محتوای فارسی خود را اضافه کنید:

```markdown
# شبکه در لینوکس

محتوای شما...
```

3. صفحه را به ناوبری در `mkdocs.yml` اضافه کنید:

```yaml
nav:
  - خانه: index.md
  - مبانی لینوکس: basics.md
  - مجوزهای فایل: permissions.md
  - شبکه: networking.md  # جدید
```

### استفاده از ویژگی‌های پیشرفته Markdown

#### جداول

```markdown
| ستون 1 | ستون 2 | ستون 3 |
|--------|--------|--------|
| داده 1 | داده 2 | داده 3 |
```

#### کد با syntax highlighting

````markdown
```python
def hello_world():
    print("سلام دنیا")
```
````

#### هشدارها (Admonitions)

```markdown
!!! warning "توجه"
    این یک پیام هشدار است

!!! info "نکته"
    این یک نکته مفید است

!!! danger "خطر"
    این یک پیام خطرناک است
```

#### Task Lists

```markdown
- [x] کار انجام شده
- [ ] کار در حال انجام
- [ ] کار آینده
```

## 🎨 سفارشی‌سازی

### تغییر رنگ‌های پوسته

در `mkdocs.yml`:

```yaml
theme:
  palette:
    primary: indigo    # تغییر به: red, blue, green, etc.
    accent: indigo     # رنگ accent
```

### افزودن CSS سفارشی

1. فایل CSS خود را در `theme/` ایجاد کنید
2. آن را به `mkdocs.yml` اضافه کنید:

```yaml
extra_css:
  - theme/styles.css
  - theme/custom.css
```

### افزودن JavaScript سفارشی

```yaml
extra_javascript:
  - theme/script.js
  - theme/custom.js
```

## 🔍 جستجو

جستجو به طور خودکار فعال است و از زبان فارسی پشتیبانی می‌کند.

میانبرهای کیبورد:
- `Ctrl+K` یا `Cmd+K`: باز کردن جستجو
- `Esc`: بستن جستجو

## 📱 طراحی واکنش‌گرا

سایت به طور خودکار با اندازه‌های مختلف صفحه سازگار است:
- 💻 دسکتاپ
- 📱 موبایل
- 📱 تبلت

## 🌙 حالت تاریک/روشن

کاربران می‌توانند بین حالت روشن و تاریک جابجا شوند. تنظیم به طور خودکار ذخیره می‌شود.

## 🐛 عیب‌یابی

### مشکل: فونت فارسی نمایش داده نمی‌شود

اطمینان حاصل کنید که فایل `styles.css` به درستی بارگذاری شده است:

```bash
# بررسی در console مرورگر
# باید خطایی در بارگذاری فونت نباشد
```

### مشکل: جستجو کار نمی‌کند

```bash
# نصب مجدد پلاگین جستجو
pip install --upgrade mkdocs-material
```

### مشکل: تغییرات نمایش داده نمی‌شود

```bash
# پاک کردن کش و بیلد مجدد
mkdocs build --clean
mkdocs serve
```

## 📚 منابع مفید

- [مستندات MkDocs](https://www.mkdocs.org/)
- [Material for MkDocs](https://squidfunk.github.io/mkdocs-material/)
- [راهنمای Markdown](https://www.markdownguide.org/)
- [Linux Professional Institute](https://www.lpi.org/)

## 🤝 مشارکت

برای مشارکت در این پروژه:

1. مخزن را Fork کنید
2. شاخه جدید ایجاد کنید: `git checkout -b feature/new-content`
3. تغییرات خود را commit کنید: `git commit -am 'Add new content'`
4. به شاخه خود push کنید: `git push origin feature/new-content`
5. Pull Request ایجاد کنید

## 📝 لیست وظایف توسعه

- [x] راه‌اندازی ساختار پایه
- [x] پیاده‌سازی RTL
- [x] ایجاد محتوای نمونه فارسی
- [x] تنظیم جستجوی فارسی
- [ ] افزودن محتوای بیشتر برای تمام موضوعات LPIC-1
- [ ] ایجاد تمرین‌های تعاملی
- [ ] افزودن ویدیوهای آموزشی

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 💬 تماس و پشتیبانی

برای سوالات و پیشنهادات:
- ایجاد Issue در GitHub
- ارسال Pull Request

---

**ساخته شده با ❤️ برای جامعه لینوکس ایران**
